---
import { PortableText } from "astro-portabletext";
import InlineImageInline from "./InlineImageInline.astro";
import Block from "./Block.astro";
import Link from "./Link.astro";
import List from "./List.astro";
import ListItem from "./ListItem.astro";

interface Props {
  value: any[];
}

const { value } = Astro.props;

const components = {
  type: {
    inlineImageInline: InlineImageInline,
  },
  block: {
    h2: Block,
    h3: Block,
    h4: Block,
    normal: Block,
    blockquote: Block,
  },
  list: {
    bullet: List,
    number: List,
  },
  listItem: {
    bullet: ListItem,
    number: ListItem,
  },
  mark: {
    link: Link,
  },
};
---

{value && value.length > 0 && (
  <div class="max-w-4xl [display:flow-root]">
    <PortableText value={value} components={components} />
  </div>
)}
